{% extends 'base.html.twig' %}

{% block title %}Formulaire de modification de mot de passe{% endblock %}
      {% form_theme myForm 'bootstrap_4_layout.html.twig' %}

{% block body %}
<br>
<br>
<br>
    <div class="container">
        <h1>Modification du mot de passe</h1>
        {{form_start(myForm)}}
            {{form_widget(myForm)}}
                <button type="submit" class="btn btn-success">Confirmez la modification du mot de passe</button>
        {{form_end(myForm)}}        
    </div>
{% endblock %}